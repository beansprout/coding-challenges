/*
 * Given a directed graph, design an algorithm to find out whether there is a route between two nodes.
 * Google "directed graph" and look under images for visual examples.
 */

// graph structure:


/* func (nodeA, nodeB) {
  if an in from nodeA matches an out from nodeB then true
  if an in from nodeB matches an out from NodeA then true
*/

class directedGraph {
  constructor (node) {

  }
}

const routeBetweenNodes = (nodeA, nodeB) => {
  if (nodeA.ins.hasOwnProperty(nodeB.outs)
  }
